"""所有 Prompt 集中管理：角色设定 + 各阶段任务 Prompt + 确认单模板。

注意：LLM 不可用时，src/analyzer.py 与 src/creator.py 会自动降级为
规则引擎（同一套六维框架），因此 Demo 在无 Key 环境下也能完整跑通。
"""

# ======================================================================
# 1. 角色设定（System Prompt）
# ======================================================================
SYSTEM_PROMPT = """你是一位精通社交媒体算法的内容策略专家。

你的核心能力是通过对比分析"爆款"与"普通"内容，提炼出可复用的成功公式，并基于此进行高质量的二次创作。
你的风格注重数据驱动、逻辑严密且极具创意。

工作准则：
1. 只用真实抓取到的公开数据说话。数据缺失就标注"数据不可得"，绝不臆造互动数字。
2. 任何结论必须能回溯到具体样本（引用原文片段 / 具体指标差值）。
3. 归因时优先寻找"可控变量"（选题角度、Hook 写法、结构、CTA、发布时段），
   而不是把差异笼统归结为"运气"或"算法波动"。
4. 二次创作必须是原创表达，只复用结构与方法论，不搬运原文句子。
5. 内容入库前必须经过人工确认，未获确认时禁止写入资产库。

输出风格：结论先行、分点、可执行，必要时用表格对比。
"""

# ======================================================================
# 2. 第二步：六维拆解
# ======================================================================
DIMENSIONS = [
    ("topic", "选题"),
    ("hook", "Hook（开头）"),
    ("structure", "结构节奏"),
    ("density", "信息密度"),
    ("cta", "CTA（行动号召）"),
    ("interaction", "互动设计"),
]

ANALYSIS_PROMPT = """请对下面这条【{group}】内容做六维拆解。

账号：{account}
发布时间：{published_at}
真实互动数据：{metrics}
正文：
\"\"\"{text}\"\"\"

规则引擎已给出初步打分与证据（可修正，但不要凭空推翻）：
{heuristics}

请严格输出 JSON（不要额外文字）：
{{
  "dimensions": {{
    "topic":       {{"score": 0-100, "insight": "为什么这个选题成立/失效", "evidence": "原文片段"}},
    "hook":        {{"score": 0-100, "insight": "开头用了什么钩子", "evidence": "原文片段"}},
    "structure":   {{"score": 0-100, "insight": "结构与节奏特征", "evidence": "段落走向概述"}},
    "density":     {{"score": 0-100, "insight": "信息密度评价", "evidence": "关键信息点数量"}},
    "cta":         {{"score": 0-100, "insight": "CTA 是否清晰有力", "evidence": "原文片段或'缺失'"}},
    "interaction": {{"score": 0-100, "insight": "互动装置设计", "evidence": "提问/投票/悬念等"}}
  }},
  "one_line": "一句话总结这条内容的胜负手"
}}"""

DIFF_PROMPT = """下面是同一账号在同一时间窗口内、由真实公开数据划分出的两组内容的量化对比。

【高表现内容】(互动数据 Top)
{high_block}

【普通内容】(对照组)
{normal_block}

规则引擎算出的变量差值：
{variable_table}

请回答：导致数据差异的关键变量是什么？请区分"强因果""疑似相关""噪声"。
严格输出 JSON：
{{
  "key_variables": [
    {{"name": "变量名", "direction": "正向/负向", "confidence": "强因果/疑似相关/噪声",
      "explanation": "结合具体样本说明", "actionable": "下次创作怎么用"}}
  ],
  "core_insight": "一句话核心洞察（要具体到这个账号的受众）",
  "rejected_hypotheses": ["被数据否掉的猜想"]
}}"""

TEMPLATE_PROMPT = """基于以下真实对比结论，沉淀一个可复用的"爆款内容结构模板"。

核心洞察：{core_insight}
关键变量：{key_variables}
高表现样本结构特征：{structure_facts}

严格输出 JSON：
{{
  "name": "模板名称（要像方法论，不要口号）",
  "applicable_scene": "适用场景",
  "blocks": [{{"role": "模块名", "goal": "这一段要达成什么", "rule": "写法约束（含字数/句式建议）"}}],
  "hook_patterns": ["可套用的开头句式1", "句式2"],
  "cta_patterns": ["可套用的CTA句式1"],
  "publish_window": "建议发布时段（基于数据）",
  "checklist": ["发布前自检项"]
}}"""

# ======================================================================
# 3. 第三步：二次创作
# ======================================================================
CREATION_PROMPT = """请基于沉淀出的模板，围绕同一个核心洞察，生成 {n} 条原创候选内容。

核心洞察：{core_insight}
结构模板：{template}
账号语境：{account}（受众画像参考高表现样本）
必须遵守：
- 三条走不同角度：① 反常识/冲突开场 ② 数据清单式 ③ 第一人称故事式
- 每条都要完整包含模板里的全部模块，长度 {min_len}-{max_len} 字
- 原创表达，禁止复述原文句子
- 每条结尾必须有明确 CTA 与一个互动装置

严格输出 JSON：
{{
  "drafts": [
    {{"angle": "角度名", "hook": "开头一句", "body": "完整正文（含换行）",
      "cta": "行动号召", "interaction_device": "互动装置", "why_it_works": "对应模板哪条规则"}}
  ]
}}"""

REVISION_PROMPT = """人工审核未通过，需要按意见重新生成 {n} 条候选内容。

审核意见（最高优先级，必须逐条落实）：
{feedback}

上一轮候选（避免重复）：
{previous}

核心洞察：{core_insight}
结构模板：{template}

输出格式与上一轮一致（JSON，drafts 数组）。"""

# ======================================================================
# 4. 第三步：人工确认提示语（流程强制暂停点）
# ======================================================================
CONFIRM_HINT = "分析已完成。请审核生成的候选内容。输入 '确认' 将内容写入资产库，输入 '修改' 重新调整。"
CONFIRM_ACCEPT = ("确认", "confirm", "y", "yes", "ok")
CONFIRM_REVISE = ("修改", "revise", "m", "edit", "no")
CONFIRM_ABORT = ("取消", "abort", "quit", "q", "exit")
