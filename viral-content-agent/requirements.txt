# ==========================================================================
# 核心链路零第三方依赖：只用 Python 标准库即可跑通完整 Demo
#   （HTTP 走 urllib 兜底、.env 自带解析器、终端渲染自研）
# 下面都是「可选增强」，按需安装。
# ==========================================================================

# 更稳的 HTTP 客户端（安装后自动启用，未安装则回退 urllib）
requests>=2.31.0

# 标准 .env 加载（未安装时使用 config/settings.py 内置解析器）
python-dotenv>=1.0.0

# 浏览器抓取通道：X / 微博 / 小红书等需要渲染的平台
# 安装后还需执行：playwright install chromium
# playwright>=1.44.0

# 测试（也可直接用标准库 unittest：python -m unittest discover -s tests）
pytest>=8.0.0

# 可视化 Web Demo（app.py）：streamlit run app.py
# pandas 由 streamlit 间接依赖，无需单独声明；若运行环境缺 pandas 可一并安装
streamlit>=1.34.0

